{
  "name": "share",
  "version": "0.0.1",
  "description": "javascript",
  "keywords": "nice",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "dev:store": "parcel store.html --open",
    "dev:user": "parcel userProfile.html --open",
    "dev:adopt": "parcel adopt.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
